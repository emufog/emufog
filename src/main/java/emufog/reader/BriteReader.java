package emufog.reader;

import com.google.common.graph.Graph;

import java.io.IOException;

public class BriteReader implements ITopologyReader {


    @Override
    public Graph read(String files) throws IOException, IllegalArgumentException {

        Graph graph = null;

        return graph;
    }
}
