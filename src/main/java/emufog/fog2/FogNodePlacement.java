package emufog.fog2;

import emufog.container.FogType;
import emufog.graph.Node;

public class FogNodePlacement extends NodePlacement {

    FogNodePlacement(Node node, FogType fogType) {
        super(node, fogType);
    }
}
